#include "bodypart.h"

bodyPart::bodyPart() {}
// clip the section that the user is drawing on, thus can have many of these clipped to the sections.
//so a bunch of draw areas widgets that allow the user to edit the skin
